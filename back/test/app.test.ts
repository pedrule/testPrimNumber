import chai from 'chai';
const expect = chai.expect;
import { number } from '../src/app';

describe('initialisation of premium test', () => {
    it('should have a new const constructed by class named CalcOfPremium', ()=>{
        expect(number).to.not.be.undefined;
    })
})